#include <stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int h, w, C[100][100];
    cin >> h >> w;
    for(int i = o; i < h; i++){
        for(int j = 0; j < w; j++)
        cin >> C[i][j];
    }
    for(int i = 0;i < h; i++){
        
    }

}