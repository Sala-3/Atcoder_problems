#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int N,A[100],B[100];
    cin >> N;
    for(int i = 1;i <= N; i++){
        cin >> A[i];
    }
    for(int j = 1;j <= N; j++){
        cin >> B[j];
    }
    for(int m = 1;m <= N;m++){
        if(j=A[i])
    }

}